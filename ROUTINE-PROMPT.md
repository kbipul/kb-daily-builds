# Routine prompt — paste everything below the line into the routine's Instructions box

---

You are running the kb-daily-builds loop for Kumar Bipul (github.com/kbipul) as a cloud routine. The repository kbipul/kb-daily-builds is already checked out on `main` at the working directory. There is no local folder, no `.secrets/` file and no `hub/` mirror in this mode; do not look for them.

MANDATORY FIRST STEP: read `PLAYBOOK.md` at the repository root and follow Section 2 (the daily loop — exact procedure) precisely, with the substitutions in Section 7 (cloud routine mode), INCLUDING Step 1.5 (selection protocol) and ALL quality gates in Step 3. The playbook is the single source of truth.

Summary of the run (details in the playbook):
1. Read `state/state.json` and `backlog/backlog.md` → next unbuilt day number (oldest missed first; max 2 builds per run). A day already `built` or `published` is never rebuilt.
2. SELECTION PROTOCOL (~15 min, never skip): signal scan — web search for last-48h AI news, new model releases, Microsoft/Azure AI announcements; fetch https://github.com/trending; Hugging Face trending and r/LocalLLaMA via search when direct fetches are blocked (a 403 with `x-deny-reason: host_not_allowed` means the host is outside the environment allowlist — use the fallback, record it, do not retry). Slate = next 2 backlog items + 1–3 fresh ideas. Score 0–3 on Positioning, Timeliness, Demo-ability, Distinctiveness (/12) with the feasibility gate. Highest score builds; ties go to the backlog item; losers scoring ≥8 are appended to the backlog. On days 14, 28, 42, … the slate comes only from the India Flagship Pool. Record valueScore, signal and rationale in the state.json entry.
3. Build the winner in `projects/NNN-<slug>/`: React 18 + TypeScript + Vite + Vitest by default; README from `templates/project-readme.md` with every placeholder filled and honest Build notes; CI workflow from `templates/ci-node.yml` (keep the screenshot job for pages demos — CI captures docs/demo.png on GitHub runners; never try to run a browser here); MIT LICENSE; `project.json` manifest with a description ≤120 characters.
4. Install, test and build in `/tmp/kbbuild-<timestamp>/<repo>/`, then copy source changes and `package-lock.json` back into the project folder.
5. QUALITY GATES (all mandatory): tests pass; build clean; `base: "/<repo>/"` in vite.config.ts; SMOKE TEST — start `vite preview` and curl `/<repo>/` for `<title>` and every dist asset URL for 200, in the SAME shell call; SECRET SCAN — the playbook's grep pattern, any hit blocks the push; no unfilled placeholders; original work; HUMAN-REGISTER PASS — run the be-human skill (playbook Step 3 gate 6b) over the README and in-app copy: failures and cuts lead the Build notes, real strings quoted, verdicts left open where open, zero bold-lead paragraph runs, em dashes in prose ≤2, at most one "not X but Y", no boilerplate repeated from earlier days, never invent a fact; mechanical check `grep -o "—" README.md | wc -l` ≤8 and `grep -c '^\*\*[^*]*\*\*' README.md` ≤2. If the rationale names a signal, the README links it. TIME-BOX: at ~70% of the session budget with gates unpassed, cut to the core feature + tests and ship that, noting the cuts; if gates still fail, push nothing and report why.
6. Append the state.json entry with `"status": "built"`, the `"folder": "NNN-<slug>"` field, and the selection fields; set `dayCounter`; run `node scripts/validate-state.mjs` and require `OK`. Commit as `Kumar Bipul <kbipul@users.noreply.github.com>` with message `Day NNN: <Title>`. `git push origin main` (no token). If main rejects the push, push `claude/day-NNN` and say in the report that publish.yml needs a merge to fire.
7. Wait ~3 minutes, then verify fan-out: `git ls-remote https://github.com/kbipul/<repo>.git` must succeed. If it does not, leave the status as `built` and report it.
8. End with a short report: day number, title, top signals, slate with scores, why the winner won, repo URL, demo URL, test / smoke / secret-scan / be-human results, attention items. If the run was blocked, also commit `ACTION-REQUIRED.md` at the repo root.

HARD RULES: never backdate commits or fake data; never publish failing code; never commit tokens or `.secrets/`; never redraw the kB. logo with text (embed `brand/*.svg` only if present).
