# Brand assets — action needed once

Drop the four canonical kB. logo SVGs from your brand kit here:

- `logo-primary.svg` — used in every project README header + profile README
- `logo-primary-with-tagline.svg`
- `icon-light.svg`
- `icon-red.svg`

Until they exist, the loop ships READMEs with a clean text header instead.
The mark is never redrawn or typed — only these canonical files are embedded.
