# Weekly Audit — 2026-W32 (Sun 2026-08-09)

kb-daily-builds · github.com/kbipul · Auditor: automated Sunday run (Bipul not present).

## ⚠️ Headline: the GitHub token is EXPIRED — the loop can't push

**Root cause found this run: the classic PAT in `.secrets/github_token.txt`
(`ghp_…`, 40 chars) is rejected for writes.** A `git push --dry-run` returns
`remote: Invalid username or token. Password authentication is not supported`,
reproducibly. Clone and `ls-remote` still "work" only because kb-daily-builds is
a **public** repo — those reads are anonymous and ignore the token entirely, so
they are not evidence the token is valid.

This single fact explains everything else:

1. **The daily build loop has produced nothing since Day 21 (2026-07-28).** The
   last successful push was Day 21's build commit (`d6c0e20`, 2026-07-28) — the
   token was still valid then. Every push since has failed auth, so no new day
   could ship and no catch-up could land. ~12 silent days.
2. **Day 21 `kb-agent-framework` never fanned out.** `git ls-remote
   https://github.com/kbipul/kb-agent-framework.git` → does not exist; status is
   still `built`. Its build is healthy (verified below) — only publication is
   missing. (Fan-out is driven by `publish.yml`'s own Actions token, so a
   secondary Action failure on the 07-28 push can't be ruled out, but it is moot
   until the PAT is restored.)
3. **This audit could not be pushed.** The audit + LinkedIn-draft files are
   written and mirrored into the local `hub/` folder so Bipul can read them, but
   the commit to `kb-daily-builds@main` is **blocked on the same expired token**.
   Per PLAYBOOK §6 Recovery ("Token invalid/expired") and §0 ("If the clone
   fails… STOP and report"), the correct action is to STOP writing and escalate,
   not to improvise.

**→ ACTION REQUIRED (Bipul): regenerate a GitHub PAT with `repo`/Contents-write
scope and paste it into `.secrets/github_token.txt` (no trailing newline).** The
moment that's done, the next scheduled run will push this audit, catch up the
missed days honestly (2 builds/run), and re-attempt Day 21's fan-out — the
Action re-syncs any project still marked `built`, so Day 21 publishes on the
first successful push. Nothing here needs a rebuild; it needs a working token.

W31's audit was also skipped (no `audits/2026-W31.md`) — consistent with the
loop having been dark since late July.

## Verdict at a glance

- **Scope note:** by the letter of §3 ("`publishedAt` within the last 7 days"),
  **nothing qualifies** — the last publish was 13 days ago. So this audit covers
  the projects new since the last real audit (W30 covered Days 12–19):
  **Day 20 (published) and Day 21 (built, unpublished)**, plus the W30 carryover.
- **All 20 published repos still exist** (`git ls-remote` sweep, 20/20 ✓).
- **W30 carryover FIXED & VERIFIED:** Day 13 `hybrid-search-ts` `docs/demo.png`
  is now present on the repo (committed 2026-07-26, `docs: demo screenshot`).
  The empty-commit CI re-trigger from W30 worked — README image resolves.
- **Day 20 contamination-scanner: clean, 5/5-ish.** 54/54 tests pass in the
  sandbox, production build OK, base path correct, `docs/demo.png` present,
  smoke test green (live Pages artifact serves), README placeholder-free with CI
  + live-demo badges and a first-class honesty note.
- **Day 21 kb-agent-framework: code is push-ready, but unpublished.** 35/35
  tests pass, clean build, `base: "/kb-agent-framework/"`, secret scan clean,
  no placeholders. Its only defect is that it isn't live (see headline).
- **Nothing scored ≤2.** No deletion/rebuild candidates.
- **Backlog healthy:** ~69 unbuilt Week 4–13 items (floor 14) + a stocked
  signal-derived pool; **7 unbuilt India Flagship ideas** (floor 4). No refill
  needed this week.
- **Selection quality (of the picks that did happen): healthy, no rubric
  recalibration recommended.** The real selection problem this week is that no
  selection ran at all.

## Scores (1–5: works · README clarity · originality · visual polish)

| Day | Repo | Works | README | Orig. | Polish | Tests | Notes |
|--:|---|:--:|:--:|:--:|:--:|--:|---|
| 20 | contamination-scanner | 5 | 5 | 5 | 4 | 54 ✓ | Client-side train/test contamination scanner (exact + n-gram + near-dup Jaccard + clean rescore). Live demo serves, demo.png present, honesty note is first-class. Rode eval-integrity signal on Timeliness 3. |
| 21 | kb-agent-framework | —* | 5 | 4 | —* | 35 ✓ | Dependency-free typed agent runtime (tools, memory, step-guarded ReAct, tracing) + zero-key Pages demo. Code verified locally; **not published**, so live "works" and "polish" can't be scored yet. |

\* Day 21 works/polish are **not scorable** until it fans out — no live demo, no
CI run, no `docs/demo.png`. Local code quality is a clean 5 (35 tests pass,
builds, correct base path, secret-clean). Score it properly next audit, once live.

**Scoring method / honesty note (unchanged from W30):** *Works* = repo present +
`npm ci && vitest run` green in the sandbox + production build + base-path/smoke
check. *README* = no unfilled `{{PLACEHOLDERS}}`, CI + demo badges, honest
Build-notes. *Visual polish* is assessed **indirectly** (the sandbox cannot run
a browser): inferred from the CI screenshot rendering cleanly (`docs/demo.png`
committed back) and a clean production build with the correct `base`. Polish caps
at 4 — a 5 needs human eyes on the live demo. I could not check GitHub stars or
Pages traffic (GitHub API blocked in the sandbox); those signals are unverified
here.

## W30 carryover — verified

- **Day 13 `hybrid-search-ts` missing `docs/demo.png` → FIXED.** Shallow-cloned
  the live repo: `docs/demo.png` (210 KB) is present, latest commit is
  `docs: demo screenshot [skip ci]` (2026-07-26). The broken README image W30
  flagged now resolves. Closes the W30 action item.

## Issues found & actions taken this run

1. **Expired GitHub PAT (CRITICAL, ROOT CAUSE).** `.secrets/github_token.txt`
   is rejected for writes (public reads still pass anonymously). This blocks the
   daily loop's pushes and this audit's push alike. **No code fix possible from
   here — the token must be regenerated by Bipul.** Audit files are written to
   the local `hub/` mirror; the hub commit is queued but unpushable until the
   token is restored. Day 21 left honestly at `built`.
2. **Backlog hygiene (minor, fixed).** `Bhasha Detect` in the India Flagship
   Pool was not marked built (it shipped Day 14). Marked it `~~built Day 14~~`
   so the pool reflects reality. Unbuilt flagship count is 7 either way (floor 4).

No code defects found in Day 20 or Day 21. Both carry MIT `LICENSE`, correct
`base`, placeholder-free READMEs, CI + demo badges, passing tests, and clean
secret scans.

## Anything scoring ≤2 (deletion / rebuild candidates)

None. Day 21 is a publish/ops failure, not a quality failure — its code is
sound. Do not rebuild it; just get it fanned out.

## Selection-quality check

Reviewed `valueScore` / `signal` / `rationale` for Days 12–21.

- **Did timely picks stay relevant?** Day 20 rode the OpenAI-benchmark-theft +
  Kimi-K3 eval-integrity moment (~Jul 21–27); by today that news is ~2 weeks
  old, but the tool is evergreen (contamination detection isn't time-bound), so
  it hasn't rotted. Day 14 flagship (bhasha-detect) is evergreen. No pick has
  become embarrassingly stale.
- **Did pure top-score dissolve the weekly arc into unrelated one-offs?** No —
  the opposite. Every rationale this window explicitly reasons about arc
  coherence and thematic overlap: Days 16→17→18→19 are a deliberate
  agent-internals progression (one tool call → one ReAct loop → multi-agent
  coordination → agent memory), Day 20 *consciously* pivots off that arc onto
  eval-trust, and Day 21 is framed as the Week-3 capstone. The rubric's failure
  mode (7 disconnected top-scorers) is not occurring.
- **Verdict: healthy. No recalibration recommended.** Caveat: the more urgent
  "selection quality" issue is that **no selection has run since Day 21** — a
  rubric that never executes can't drift, but it also can't build credibility.

## Backlog status

- **India Flagship Pool:** 7 unbuilt (Hinglish Bridge, Kisan Sahayak, UPI Guard,
  India AI Pulse, Indic OCR Lab, Indic PII Redactor, Anuvaad Checker). Floor 4 ✓.
- **General backlog:** Weeks 4–13 (Days 22–90) unbuilt ≈ 69 planned, plus a
  stocked signal-derived candidate pool (skill-lint, foundry-model-board,
  ai-risk-tier, skill-portability, rag-injection-scanner, tool-call-firewall,
  foundry-residency, price-floor-board, loop-guard). Floor 14 ✓.
- **No refill required this week.** Next mandatory refill trigger is when ≤14
  days remain in the main arc.

## Recommendations to Bipul (priority order)

1. **Regenerate the GitHub PAT and paste it into `.secrets/github_token.txt`**
   (classic `repo` scope, or fine-grained with Contents: Read *and Write* on
   kb-daily-builds; no trailing newline). This is the whole blocker — do it
   first. Everything else flows from a working token.
2. **After the token is fixed, confirm the 6 AM scheduled session actually
   fires.** A dead token and a dead scheduler look identical from silence; verify
   both. The next run should push this audit and start honest catch-up.
3. **Then check https://github.com/kbipul/kb-daily-builds/actions** for the Day
   21 (2026-07-28) `publish.yml` run and confirm `kb-agent-framework` fans out on
   the first good push. If it still doesn't, the Action itself needs a look.
4. **Catch up honestly** (2 builds/run per the catch-up rule) — never backfill
   dates or fake the streak. Nothing to delete or rebuild; backlog + rubric are
   healthy.
